# CLAUDE.md — 꿀퀴즈 인더바이블

> version: v2.0 | 2026-09-08 | 540 Into Insight

## 이 폴더가 무엇인가

퍼듀교회 양육팀의 성경통독 프로그램 **꿀보다 성경**에 딸린 일일 퀴즈 웹앱.
교인이 그날 읽은 본문으로 열 문항을 풀고, 마지막에 암송 구절 한 절을 받는다.

- 배포처: **GitHub Pages** — https://karamjang-arch.github.io/honeybible/
- 빌드 없음. 프레임워크 없음. 순수 HTML/CSS/JS.
- 로그인 없음. 저장소(localStorage 포함) 사용 금지. 어떤 기록도 남기지 않는다.
- 통독 기간: 2026-09-01 ~ 2027-04-30 (242일)

## v2.0에서 바뀐 것 (2026-09-08)

Netlify에서 GitHub Pages로 옮기고, 문항을 `index.html` 밖으로 분리했다.

| | v1 (Netlify) | v2 (GitHub Pages) |
|:--|:--|:--|
| 문항 위치 | index.html 안 BANK | days/*.json |
| 갱신 비용 | 배포당 15크레딧 | 0 |
| 파일 크기 | 날마다 누적 | 일정 (~20KB) |
| 배포 | `netlify deploy` | `git push` |

**갱신 비용이 0이므로 오타 하나도 그 자리에서 고쳐 올린다.** 배포 횟수를 아끼지 않는다.

## 파일 구조

```
honeybible/
├── index.html              화면 코드. 문항 추가 시 수정하지 않음
├── days/
│   ├── index.json          날짜 목록 (최신순)
│   └── YYYY-MM-DD.json     하루치 문항
├── CLAUDE.md               이 문서
├── verify.mjs              검증 스크립트
└── .claude/commands/honey.md
```

## 역할 분담 (어기지 말 것)

| 주체 | 하는 일 |
|:--|:--|
| claude.ai 540 프로젝트 | 문항 출제, 오답 설계, 해설 집필, 암송 구절 선정 |
| **Claude Code (이 폴더)** | JSON 생성, 목록 갱신, 검증, 커밋·푸시 |
| 리더님 | 문항 검수, 배포 승인 |

**CC는 문항을 새로 짓지 않는다.** 리더님이 붙여넣은 내용을 그대로 넣는 것이 기본이다.
문항에 명백한 오류(성경 사실, 개역개정 표기)를 발견하면 **고치지 말고 보고**한다.

## 데이터 규칙

1. 파일명은 `days/YYYY-MM-DD.json`. 파일 안의 `date` 값과 **반드시 일치**한다.
2. `days/index.json` 에 같은 날짜를 한 줄 추가한다. 최신 날짜가 위. `passage` 는 날짜 파일과 글자까지 동일하게.
3. 기존 날짜 파일은 **건드리지 않는다.** 과거 퀴즈는 그대로 살아 있어야 한다.
4. 같은 날짜가 이미 있으면 덮어쓰지 말고 리더님께 확인한다.
5. `a` 는 정답이 `opts` 에서 몇 번째인지(0부터). **보기 순서는 화면에서 매번 섞이므로 정답 위치를 조정하지 않는다.**
6. 문항은 10개가 기본. 하루 3~4장 분량 기준.

## 화면 동작 (index.html)

- 첫 화면은 **오늘 퀴즈 표지**. 달력이 아니다. 지난 퀴즈는 아래쪽 작은 링크로만 들어간다.
- 오늘 날짜는 **America/Indiana/Indianapolis** 기준. 교인이 해외에 있어도 교회와 같은 날을 본다.
- **미래 날짜는 잠긴다.** 목록에 있어도 달력에서 꺼지고, 주소(`#2026-09-20`)로 접근해도 안내 문구가 뜬다.
  단, 이는 서버 검증이 아니라 화면 단의 예의 장치다. 기기 시계를 바꾸면 볼 수 있다. 교회 용도에서는 이 정도로 충분하다고 판단했다.
- 문항 파일을 미리 만들어 올려 두어도 그날이 되어야 열린다.

## 절대 금지

- **개역개정 본문 수정.** `hintText` 와 `memory.text` 는 한 글자도 바꾸지 않는다. 확신이 없으면 리더님께 확인하고, 임의로 채우지 않는다.
- localStorage, sessionStorage, 쿠키, 외부 분석 스크립트 추가
- 점수·순위·기록 기능 추가 (통독의 목적을 훼손함)
- 검증 실패 상태에서의 푸시
- 문항 추가를 이유로 `index.html` 수정

## 작업 순서 (게이트)

```
문항 접수
  → days/YYYY-MM-DD.json 생성
  → days/index.json 에 한 줄 추가
  → node verify.mjs        ← 통과 못 하면 여기서 멈춤
  → 리더님께 결과 보고 + 승인 요청
  → 승인 후에만 git push
```

**검증을 건너뛰고 올리지 않는다.** 리더님이 "바로 올려줘"라고 해도 verify는 돌린다.

## 명령어

```bash
# 검증
node verify.mjs

# 로컬 확인 (더블클릭으로는 days/ 를 못 읽는다)
python3 -m http.server 8000    # → http://localhost:8000

# 배포
git add -A && git commit -m "add YYYY-MM-DD 본문" && git push
```

## 출제 규격 (검증 시 참고)

CC가 문항을 짓지는 않지만, 명백한 위반을 발견하면 보고한다.

- 문항 범위: 성경 인물의 이름 / 중요한 지명 / 하나님 약속의 핵심 / 사건
- 출제 금지: 숫자, 연대, 족보 순서, 원어 문법
- **오답은 거짓 교리가 아니라 다른 참된 사실이어야 한다.** 오답을 읽는 것만으로 잘못된 각인이 남으면 안 된다.
- 오답은 같은 범주에서 뽑는다. 선지자 문제면 오답도 선지자, 왕 문제면 오답도 같은 왕조.
- **핵심 교리 문장을 오답으로 쓰지 않는다.**
- **한 장면에서 같은 대상에게 하신 말씀끼리는 서로의 오답이 될 수 없다.** 오답은 다른 대상에게 하신 말씀에서 가져오고, 질문에 대상과 정황을 명시한다.
- 해설은 답을 밝히는 문장 뒤에 **(창 19:16)** 형식으로 절 표기를 붙인다. (2026-09-07 확정)
- 본문이 한 사람의 행동만 기록했다고 해서 다른 사람이 그리하지 않았다고 단정하지 않는다.
- 회복(Restoration)을 신설(Institution)로 쓰지 않는다.
- 해설은 명령형(`~해야 합니다`)으로 닫지 않는다. 서술 또는 소망형.
- Being → Doing 순서를 지킨다.

> 상세 판정례 14건은 `2026-09-08_꿀퀴즈_출제규격서_v1.0.md` 참조
> (옵시디언: 400 Purdue_Church/413 양육교재/꿀퀴즈/)

## 이력

- **v2.0 (2026-09-08)** GitHub Pages 이전. 문항을 days/*.json 으로 분리. 첫 화면을 오늘 퀴즈로, 지난 퀴즈는 링크로. 미래 날짜 잠금. 교회 시간대 고정.
- v1.0 (2026-09-02) 최초 작성. 단일 파일 구조, Netlify 배포.
